{
  "name": "input-autocomplete",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "tsc --noEmit && ts-node config/esbuild-server.ts",
    "dev": "tsc --noEmit && ts-node config/esbuild-watch.ts",
    "build": "tsc --noEmit && esbuild src/autocomplete.ts --bundle --minify --outdir=dist"
  },
  "keywords": [
    "autocomplete"
  ],
  "author": "awenn2015",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.1.0",
    "esbuild": "^0.17.18",
    "express": "^4.18.2",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
